Vagrant.configure("2") do |config|
  config.vm.box = "centos/7"
  config.vm.hostname = "bsuiteauto"

  config.vm.provider "virtualbox" do |vb|
    vb.gui = true
    vb.memory = "2048" # Increase memory for better performance with GUI
  end
end